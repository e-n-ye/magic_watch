# Agent 卡片草稿池

本文件用于并行规划窗口记录新需求或草稿卡片。

执行窗口正在运行时，其他窗口可以向本文件追加内容，但不得修改当前批次、当前会话块或正在执行的卡片。

## 使用规则

- 新需求先写入本文件，等待用户确认批次和依赖。
- 确认后的卡片再移动到 `agent-plan.md`、`agent-queue.md` 或 `cards/`。
- 不在本文件维护 DONE / BLOCKED 状态。

## 草稿

空。
